on: 
   workflow_dispatch:

jobs:
  new_push_job:
    runs-on: ubuntu-latest
    name: New push to repo
    steps:
    - name: Send GitHub trigger payload to Slack Workflow Builder
      id: slack
      uses: slackapi/slack-github-action@v1.23.0
      with:
        channel-id: 'C058K4JRMGR' # ID of Slack Channel you want to post to
        payload: "{\"text\": \"posting from a github action\"}"
      env:
        SLACK_BOT_TOKEN: xoxb-5278384501347-5292112213217-mA5IBabFB0o03TDlQY4gHNyX
